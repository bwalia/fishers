#!/usr/bin/env bash
# $3 is 1 for a branch checkout, 0 for a single-file one — a file checkout
# cannot add sources to the target, so there is nothing to resync.
set -euo pipefail
[ "${3:-1}" = "1" ] || exit 0
exec "$(git rev-parse --show-toplevel)/scripts/git-hooks/sync-xcodeproj.sh"

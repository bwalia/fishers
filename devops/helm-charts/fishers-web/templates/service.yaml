apiVersion: v1
kind: Service
metadata:
  name: {{ include "fishers-web.name" . }}
  labels:
    {{- include "fishers-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "fishers-web.selectorLabels" . | nindent 4 }}

# acc ring — see values.yaml for everything not repeated here.
env: acc
hostname: acc.fishers.cloud
replicaCount: 2

image:
  repository: nebulacr.workstation.co.uk/fishers/fishers-web
  tag: latest

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    memory: 768Mi

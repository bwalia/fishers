apiVersion: v1
kind: Service
metadata:
  name: {{ include "fishers-api.name" . }}
  labels:
    {{- include "fishers-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "fishers-api.selectorLabels" . | nindent 4 }}

# test ring — see values.yaml for everything not repeated here.
env: test
hostname: test.fishers.cloud
replicaCount: 1

image:
  repository: nebulacr.workstation.co.uk/fishers/fishers-api
  tag: latest

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    memory: 512Mi

config:
  rustLog: "fishers_api=debug,tower_http=warn,sqlx=warn"
